import 'dart:ui';

class AppColors {
  // アプリで共通利用する色を複数持っているクラス
  // AppColors.mainBeigeのように呼び出せる。
  static const backgroundColor = Color(0xFFFFFDF8);
  static const mainBlue = Color(0xFF4483FF);

  static const textBlack = Color(0xff3D3D3D);

  static const error = Color(0xffF28385);
}
